Saved AI answers for `LLM_PROVIDER=replay` (Playwright and demo runs), named
`<output name>.json`: contract extraction, brand mentions (for
`fixtures/posts.csv`, one batch, oldest post first), brand merges and a pitch.
Preflight's AI checks show "couldn't check" in replay mode.
